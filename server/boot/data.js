'use strict';

module.exports = function(app) {
  /* Initialize roles, admin user
     Customize before first boot
     COMMENT/REMOVE AFTER first boot
  */
  // app.models.User.create({
  //   name: 'full name',
  //   email: 'email@admin.fake',
  //   realm: 'backend',
  //   username: 'username',
  //   emailVerified: true,
  //   password: 'password'
  // });

  // app.models.Role.create({ name: 'admin', description: 'Administrateur' });

  // app.models.RoleMapping.create({ principalType: 'USER', principalId: 1, roleId: 1 });
};
